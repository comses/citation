# Handoffs

Use this folder for transfer documents between agents or sessions.

Each handoff should include:
- current status
- key decisions
- pending tasks
- exact `make ...` and/or `docker compose run --rm test ...` commands for continuation
